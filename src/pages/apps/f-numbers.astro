---
import { getCollection, render } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import FNumbersSimulator from '../../components/islands/FNumbersSimulator.astro';

const apps = await getCollection('apps');
const entry = apps.find(a => a.id.replace(/\.mdx?$/, '') === 'f-numbers-simulator');

if (!entry) throw new Error('f-numbers-simulator content entry not found');

const { Content } = await render(entry);
---

<BaseLayout title={entry.data.title} description={entry.data.description}>
  <div class="container">
    <header class="article-header">
      <div class="article-header__meta">
        <a href="/apps" class="back-link">← Apps</a>
        <span class="tag">3D / WebGL</span>
      </div>
      <h1>{entry.data.title}</h1>
    </header>
  </div>

  <div class="app-container container">
    <FNumbersSimulator />
  </div>

  <div class="container article-body prose">
    <Content />
  </div>
</BaseLayout>

<style>
  .app-container {
    margin-bottom: 3rem;
  }

  .back-link {
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--transition);
  }

  .back-link:hover {
    color: var(--color-primary);
  }
</style>
